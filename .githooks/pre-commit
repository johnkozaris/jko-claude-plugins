#!/bin/bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

if ! command -v python3 >/dev/null 2>&1; then
  echo "python3 is required to run scripts/check_plugin_manifests.py" >&2
  exit 1
fi

python3 scripts/check_plugin_manifests.py
